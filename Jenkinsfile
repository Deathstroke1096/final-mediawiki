pipeline {
    agent {label 'linux'}

    stages {
        stage('Create Stack') {
            steps {
                echo 'Creating Stack'
                sh "echo hello from shell"
            }
        }
                
    }
}