pipeline {
    agent any

    stages {
        stage('Create Stack') {
            steps {
                echo 'Creating Stack'
                sh 'sh createStack.sh'
            }
        }
                
    }
}